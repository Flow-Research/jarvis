# CLI Fixture Snapshot Path

This directory holds release snapshots of Jarvis v0.1 conformance fixtures for
the protocol helper CLI.

Canonical fixtures live in `docs/conformance/fixtures`.

Snapshot updates require `python3 scripts/check_conformance_fixtures.py`.
